#include<iostream>
#include<vector>
using namespace std;
vector<int> unionArray(vector<int>& nums1, vector<int>& nums2) {
        int i=0;j=0;
        int m=nums1.size();
        int n=nums2.size();
        while(i<m && j<n){
            if(nums1[i])
            if(unionArray.empty()||unionArray.back()!=nums1[i]){
                unionArray.push_back(nums1[i]);
            }
            if()
        }
    }
int main()
{vector<int>num1={1,2,3,4,5};
vector<int>num2={3,4,7};



    return 0;
}